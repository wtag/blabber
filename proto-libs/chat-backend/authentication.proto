syntax = "proto3";

package GrpcClientAuthentication;

message AccessToken {
  string token = 1;
}

message User {
  int64 id = 1;
  string tenant = 2;
  string isCustomer = 3;
}

service Authentication {
  rpc RetrieveAccessToken(User) returns (AccessToken) {}
}
